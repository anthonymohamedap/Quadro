  <Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:QuadroApp.ViewModels"
        xmlns:m="using:QuadroApp.Model.DB"
        
        x:Class="QuadroApp.Views.BulkLijstenWindow"
        x:DataType="vm:BulkLijstenViewModel"
        Width="1100"
        Height="720"
        MinWidth="900"
        MinHeight="600"
        CanResize="True"
        WindowStartupLocation="CenterOwner"
        Title="Bulklijsten"
        Background="{DynamicResource SurfaceMuted}">

    <Grid RowDefinitions="Auto,*">
        <Border Background="{DynamicResource BrandDark}" MinHeight="70">
            <DockPanel Margin="20,0">
                <Button Content="Sluiten"
                        Command="{Binding SluitenCommand}"
                        DockPanel.Dock="Right"
                        VerticalAlignment="Center"
                        MinWidth="100"
                        Classes="AccentBtn"
                        Padding="10,5"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                    <Ellipse Width="35" Height="35" Fill="{DynamicResource BrandYellow}"/>
                    <TextBlock Text="QUADRO" Classes="h2" Foreground="{DynamicResource TextOnDark}" VerticalAlignment="Center"/>
                    <TextBlock Text="BULK LIJSTEN" Classes="body" Foreground="{DynamicResource TextMutedOnDark}" Margin="20,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid DataContext="{Binding}"
                  x:DataType="vm:BulkLijstenViewModel"

                  ColumnDefinitions="1.25*,1*"
                  ColumnSpacing="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Kies welke velden je in bulk wilt bijwerken"
                                   Classes="h3"
                                   TextWrapping="Wrap"/>

                        <Border Background="{DynamicResource InfoBg}"
                                BorderBrush="{DynamicResource InfoBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <StackPanel Spacing="8">
                                <CheckBox Content="Artikelnummer bijwerken"
                                          Foreground="Black"
                                          IsChecked="{Binding ArtikelnummerVeld.Bijwerken}"/>
                                <TextBox Text="{Binding ArtikelnummerVeld.Waarde, Mode=TwoWay}"
                                         Foreground = "Black"  Watermark="Nieuw artikelnummer"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InfoBg}"
                                BorderBrush="{DynamicResource InfoBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <StackPanel Spacing="8">
                                <CheckBox Content="Levcode bijwerken"
                                          Foreground="Black"
                                          IsChecked="{Binding LevcodeVeld.Bijwerken}"/>
                                <TextBox Text="{Binding LevcodeVeld.Waarde, Mode=TwoWay}"
                                         Foreground = "Black"  Watermark="Nieuwe levcode"/>
                            </StackPanel>
                        </Border>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Grid.Column="0"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Leverancier bijwerken"
                                              IsChecked="{Binding LeverancierVeld.Bijwerken}" Foreground = "Black" />
                                    <ComboBox ItemsSource="{Binding BulkLeveranciers}" Foreground = "Black" 
                                              SelectedItem="{Binding LeverancierVeld.Waarde, Mode=TwoWay}"
                                              HorizontalAlignment="Stretch">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate x:DataType="m:Leverancier">
                                                <TextBlock Foreground = "Black"  Text="{Binding Naam}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Breedte (cm) bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding BreedteCmVeld.Bijwerken}"/>
                                    <NumericUpDown Value="{Binding BreedteCmVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   Increment="1"
                                                   FormatString="0"
                                                   Minimum="0"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Background="{DynamicResource InfoBg}"
                                BorderBrush="{DynamicResource InfoBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <StackPanel Spacing="8">
                                <CheckBox Content="Soort bijwerken"
                                          Foreground="Black"
                                          IsChecked="{Binding SoortVeld.Bijwerken}"/>
                                <TextBox Text="{Binding SoortVeld.Waarde, Mode=TwoWay}"
                                         Foreground="Black"
                                         Watermark="Nieuwe soort"/>
                            </StackPanel>
                        </Border>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Grid.Column="0"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Dealerstatus bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding IsDealerVeld.Bijwerken}"/>
                                    <CheckBox Content="Instellen als dealer"
                                              Foreground="Black"
                                              IsChecked="{Binding IsDealerVeld.Waarde}"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Opmerking bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding OpmerkingVeld.Bijwerken}"/>
                                    <TextBox Text="{Binding OpmerkingVeld.Waarde, Mode=TwoWay}"
                                             Foreground="Black"
                                             Watermark="Nieuwe opmerking"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Background="{DynamicResource SuccessBg}"
                                BorderBrush="{DynamicResource SuccessBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <StackPanel Spacing="8">
                                <CheckBox Content="Prijs per meter bijwerken"
                                          Foreground="Black"
                                          IsChecked="{Binding BijwerkPrijsPerMeter}"/>
                                <CheckBox Content="Gebruik percentage (%)"
                                          Foreground="Black"
                                          IsChecked="{Binding GebruikPercentage, Mode=TwoWay}"/>
                                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                                    <StackPanel Grid.Column="0"
                                                IsVisible="{Binding IsAbsolutePrijs}"
                                                Spacing="4">
                                        <TextBlock Text="Nieuwe prijs per meter (€)" Classes="label" TextWrapping="Wrap"/>
                                        <NumericUpDown Value="{Binding NieuwePrijsPerMeter, Mode=TwoWay}"
                                                       Foreground="Black"
                                                       Minimum="0"
                                                       Increment="0.50"
                                                       FormatString="0.00"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                IsVisible="{Binding GebruikPercentage}"
                                                Spacing="4">
                                        <TextBlock Text="Prijswijziging (%)" Classes="label" TextWrapping="Wrap"/>
                                        <NumericUpDown Value="{Binding PrijsWijzigingPct, Mode=TwoWay}"
                                                       Foreground="Black"
                                                       Increment="1"
                                                       FormatString="0.00"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Grid.Column="0"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Winstfactor bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding WinstFactorVeld.Bijwerken}"/>
                                    <CheckBox Content="Winstfactor leegmaken"
                                              Foreground="Black"
                                              IsChecked="{Binding WinstFactorVeld.Leegmaken}"/>
                                    <NumericUpDown Value="{Binding WinstFactorVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   IsEnabled="{Binding !WinstFactorVeld.Leegmaken}"
                                                   Increment="0.05"
                                                   FormatString="0.000"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Afvalpercentage bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding AfvalPercentageVeld.Bijwerken}"/>
                                    <CheckBox Content="Afvalpercentage leegmaken"
                                              Foreground="Black"
                                              IsChecked="{Binding AfvalPercentageVeld.Leegmaken}"/>
                                    <NumericUpDown Value="{Binding AfvalPercentageVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   IsEnabled="{Binding !AfvalPercentageVeld.Leegmaken}"
                                                   Increment="0.25"
                                                   FormatString="0.00"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Grid.Column="0"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Vaste kost bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding VasteKostVeld.Bijwerken}"/>
                                    <NumericUpDown Value="{Binding VasteKostVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   Increment="0.50"
                                                   FormatString="0.00"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Werkminuten bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding WerkMinutenVeld.Bijwerken}"/>
                                    <NumericUpDown Value="{Binding WerkMinutenVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   Increment="1"
                                                   FormatString="0"
                                                   Minimum="0"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Grid.Column="0"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Voorraad bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding VoorraadMeterVeld.Bijwerken}"/>
                                    <NumericUpDown Value="{Binding VoorraadMeterVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   Increment="0.50"
                                                   FormatString="0.00"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource InfoBg}"
                                    BorderBrush="{DynamicResource InfoBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10">
                                <StackPanel Spacing="8">
                                    <CheckBox Content="Inventariskost bijwerken"
                                              Foreground="Black"
                                              IsChecked="{Binding InventarisKostVeld.Bijwerken}"/>
                                    <NumericUpDown Value="{Binding InventarisKostVeld.Waarde, Mode=TwoWay}"
                                                   Foreground="Black"
                                                   Increment="0.50"
                                                   FormatString="0.00"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Background="{DynamicResource InfoBg}"
                                BorderBrush="{DynamicResource InfoBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <StackPanel Spacing="8">
                                <CheckBox Content="Minimumvoorraad bijwerken"
                                          Foreground="Black"
                                          IsChecked="{Binding MinimumVoorraadVeld.Bijwerken}"/>
                                <NumericUpDown Value="{Binding MinimumVoorraadVeld.Waarde, Mode=TwoWay}"
                                               Foreground="Black"
                                               Increment="0.50"
                                               FormatString="0.00"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="1"
                        Background="White"
                        BorderBrush="{DynamicResource BorderSubtle}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10">
                    <Grid RowDefinitions="Auto,Auto,*,Auto" RowSpacing="10">
                        <TextBlock Text="Selecteer doel-lijsten"
                                   Classes="h3"
                                   TextTrimming="CharacterEllipsis"/>

                        <StackPanel Grid.Row="1" Spacing="8">
                            <TextBox Watermark="Zoek op artikelnummer of leverancier"
                                     Foreground="Black"
                                     Text="{Binding Zoekterm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <ComboBox ItemsSource="{Binding SoortOptions}"
                                      Foreground="Black"
                                      SelectedItem="{Binding SoortFilter, Mode=TwoWay}"/>
                            <TextBlock Text="{Binding SelectedCount, StringFormat='Geselecteerd: {0}'}"
                                       Classes="body"/>
                        </StackPanel>

                        <Border Grid.Row="2"
                                Background="{DynamicResource SurfaceMutedAlt}"
                                BorderBrush="{DynamicResource BorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="0">
                            <Grid RowDefinitions="Auto,*">
                                <Border Background="{DynamicResource SurfaceHeader}"
                                        Padding="10,6"
                                        BorderBrush="{DynamicResource BorderSubtle}"
                                        BorderThickness="0,0,0,1">
                                    <Grid ColumnDefinitions="1.3*,1.2*,0.9*,0.8*" ColumnSpacing="10">
                                        <TextBlock Text="Artikelnummer" Classes="label" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="Leverancier" Classes="label" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="Soort" Classes="label" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="3" Text="Prijs/m" Classes="label" TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </Border>

                                <ListBox Grid.Row="1"
                                         x:Name="BulkLijstenList"
                                         ItemsSource="{Binding FilteredLijsten}"
                                         SelectionMode="Multiple"
                                         SelectionChanged="BulkLijstenList_SelectionChanged"
                                         BorderThickness="0"
                                         Background="Transparent">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate x:DataType="m:TypeLijst">
                                            <Border Padding="10,6">
                                                <Grid ColumnDefinitions="1.3*,1.2*,0.9*,0.8*" ColumnSpacing="10">
                                                    <TextBlock Text="{Binding Artikelnummer}" Classes="body" FontWeight="Bold" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Artikelnummer}"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding Leverancier.Naam}" Classes="body" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Leverancier.Naam}"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding Soort}" Classes="body" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Soort}"/>
                                                    <TextBlock Grid.Column="3" Text="{Binding PrijsPerMeter, StringFormat='{}{0:0.00}'}" Classes="body" TextTrimming="CharacterEllipsis"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Row="3"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Spacing="10">
                            <Button Content="Reset velden"
                                    Command="{Binding ResetBulkVeldenCommand}"
                                    Foreground="Black"
                                    Classes="MainBtn"/>
                            <Button Content="Uitvoeren"
                                    Command="{Binding ExecuteActionCommand}"
                                    Foreground="Black"
                                    Classes="MainBtn"/>
                            <Button Content="Annuleren"
                                    Command="{Binding SluitenCommand}"
                                    Foreground="Black"
                                    Classes="MainBtn"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>

</Window>



